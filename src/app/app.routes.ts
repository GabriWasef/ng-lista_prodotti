// Definizione delle rotte dell'applicazione.
// Al momento non sono presenti rotte: l'intera UI è contenuta
// nel componente radice App senza navigazione tra pagine separate.

import { Routes } from '@angular/router';

export const routes: Routes = [];
